{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "HocusFocus",
            "type": "coreclr",
            "request": "launch",
            "preLaunchTask": "build",
            "program": "dotnet",
            "args": ["run", "--project", "${workspaceFolder}/HocusFocus.csproj"],
            "cwd": "${workspaceFolder}",
            "stopAtEntry": false,
            "console": "internalConsole"
        }
    ]
}
